# HR-RAG: AI-система подбора персонала

AI-powered HR система для автоматизированного анализа резюме кандидатов с использованием LLM.

##  Возможности

-  **Парсинг резюме** — автоматическое извлечение структуры из PDF/DOCX/TXT
-  **Умный анализ** — оценка соответствия кандидата вакансии по 7 критериям
-  **Kanban-доска** — управление воронкой найма (new → review → interview → offer/rejected/reserve)
-  **Сравнение кандидатов** — визуальное сопоставление до 3 кандидатов с радарными диаграммами
-  **Аналитика** — метрики воронки, конверсии, топ недостающих навыков, временные тренды
-  **Комментарии с тегами** — внутренняя коммуникация по кандидатам (@mention, #hashtags)
-  **Экспорт в PDF** — генерация детальных отчётов с поддержкой кириллицы
-  **История статусов** — полный трекинг изменений в воронке найма
-  **Настраиваемые промпты** — загрузка собственных system_prompt и hr_guidelines

##  Технологический стек

- **Frontend**: Streamlit 1.40+
- **Backend**: Python 3.11, SQLAlchemy
- **Database**: SQLite
- **LLM**: Собственный оркестратор на базе llama.cpp с поддержкой нескольких моделей
- **Document Parsing**: PyPDF2, python-docx
- **PDF Generation**: ReportLab
- **Visualization**: Plotly
- **Deployment**: Docker + docker-compose

##  Архитектура проекта
```
hr-rag/
├── app/
│ ├── components/ # UI компоненты Streamlit
│ │ ├── kanban.py # Kanban доска с drag-and-drop
│ │ ├── filters.py # Фильтры и поиск кандидатов
│ │ ├── comparison.py # Сравнение до 3 кандидатов
│ │ ├── charts.py # Графики и визуализация
│ │ ├── comments.py # Комментарии с тегами
│ │ └── status_manager.py # Управление статусами воронки
│ │
│ ├── services/ # Бизнес-логика
│ │ ├── llm_client.py # Клиент для LLM оркестратора
│ │ └── document_parser.py # Парсинг PDF/DOCX/TXT
│ │
│ ├── db/
│ │ └── models.py # SQLAlchemy модели (Vacancy, Match, Comment, StatusHistory)
│ │
│ ├── utils/
│ │ ├── metrics.py # Расчёт бизнес-метрик и конверсий
│ │ └── search.py # Поиск и фильтрация данных
│ │
│ ├── pages/
│ │ └── analytics.py # Страница аналитики с графиками
│ │
│ ├── fonts/ # DejaVu шрифты для PDF (кириллица)
│ ├── config.py # Конфигурация моделей и настроек
│ ├── pdf_export.py # Генерация PDF отчётов
│ └── streamlit_app.py # Главный файл приложения
│
├── prompts/
│ ├── system_prompt.txt # Системный промпт для LLM
│ └── hr_guidelines.txt # HR руководство по оценке
│
├── data/db/ # База SQLite (создаётся автоматически)
│
├── docker-compose.yml # Docker конфигурация
├── Dockerfile # Образ приложения
├── requirements.txt # Python зависимости
├── .env.example # Шаблон переменных окружения
├── LICENSE # MIT лицензия
└── README.md # Документация
```

##  Быстрый старт

### Требования

- Docker 20.10+
- docker-compose 1.29+
- Доступ к LLM-оркестратору (llama.cpp с OpenAI-compatible API)

### Установка

**1. Клонируйте репозиторий:**

```bash
git clone https://github.com/greebo3661/HR-analysis-system.git
cd HR-analysis-system
2. Создайте .env файл:

bash
cp .env.example .env
nano .env  # Отредактируйте параметры
3. Настройте переменные окружения:

text
LLM_MANAGER_URL=http://your-llm-server:8000
LLM_API_KEY=your-api-key-here
DATABASE_URL=sqlite:///data/db/hr_analysis.db
4. Запустите контейнеры:

bash
docker compose build
docker compose up -d
5. Откройте в браузере:

text
http://localhost:8501
 Поддерживаемые LLM модели
Система работает с любыми моделями через OpenAI-compatible API. По умолчанию поддерживаются:

a-vibe (8K context) — быстрая универсальная модель

qwen3-14b (8K context) — точная модель с reasoning

qwen2.5-coder-14b (8K context) — специализирована на технических навыках

LLM Orchestrator API
bash
# Статус оркестратора
GET /status

# Список моделей
GET /models

# Переключение модели
POST /switch/{model_id}

# Генерация (OpenAI-compatible)
POST /v1/chat/completions
 Система оценки кандидатов
Основные метрики (0-100)
Взвешенные критерии:

Hard Skills (вес 65%) — соответствие техническим требованиям

Experience (вес 35%) — релевантный опыт работы

Финальная оценка:

text
Overall Score = Hard Skills × 0.65 + Experience × 0.35
Дополнительные метрики (не влияют на Overall):

Cultural Fit — соответствие культуре компании

Communication — коммуникативные навыки

Growth Potential — потенциал роста

Stability — стабильность карьеры (40-60 = норма, 85-95 = риск)

Рекомендации по найму
Рекомендация	Условие	Действие
✅ YES	Overall ≥ 75%, нет red flags	Пригласить на интервью
🔍 MAYBE	Overall 50-74%	Уточнить детали, дополнительная оценка
❌ NO	Overall < 50% или критичные риски	Отклонить
Уровень уверенности
HIGH — чёткое соответствие/несоответствие всем критериям

MEDIUM — есть неоднозначности, требуется экспертная оценка

LOW — недостаточно данных для принятия решения

 Структура базы данных
Таблицы
vacancies — вакансии

id, title, company, requirements_json, created_at

matches — результаты анализа

id, resume_name, vacancy_id, vacancy_title, score, analysis_json, status, created_at, status_updated_at

comments — комментарии к кандидатам

id, match_id, author, text, created_at

status_history — история изменений статусов

id, match_id, old_status, new_status, changed_at

Статусы воронки найма
new  — новые кандидаты

review  — на рассмотрении

interview  — назначено интервью

offer  — сделано предложение

rejected  — отклонены

reserve  — в резерве

 Кастомизация
Промпты
Система позволяет загружать собственные промпты через интерфейс:

Боковая панель → " Настройки промптов"

system_prompt.txt — базовая инструкция для LLM

hr_guidelines.txt — правила оценки кандидатов

Модели
Добавьте новые модели в app/config.py:

python
AVAILABLE_MODELS = {
    "your-model": {
        "name": "Your Model",
        "description": "Model description",
        "model_id": "your-model"
    }
}

 Разработка
Запуск без Docker
bash
# Установите зависимости
pip install -r requirements.txt

# Запустите приложение
cd app
streamlit run streamlit_app.py
Переменные окружения
text
LLM_MANAGER_URL=http://localhost:8000
LLM_API_KEY=your-key
DATABASE_URL=sqlite:///data/db/hr_analysis.db

 Roadmap
 Поддержка PostgreSQL

 Интеграция с почтой (автоматические письма кандидатам)

 REST API для интеграций

 Телеграм-бот для уведомлений

 Расширенная аналитика с ML-предсказаниями

 Multi-tenant режим (несколько компаний)

 Участие в разработке
Pull requests приветствуются! Для крупных изменений сначала откройте issue для обсуждения.

 Лицензия
MIT License

 Автор
greebo3661

GitHub: @greebo3661

Версия: 1.0
Последнее обновление: Январь 2026
